public Boolean isPrime(Integer num) {
    //code here
   Integer remainder = Math.mod(num, 1);
   Integer remainder1 = Math.mod(num, num);

   if(remainder == 0 && remainder1 == 0){
       return true;
   }else{
       false;
   }

}