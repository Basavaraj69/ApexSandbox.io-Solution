public Boolean isLeapYear(Integer year) {
    //code here
      Integer ear = Math.mod(year, 4);
    Integer ear1 = Math.mod(year, 100);
    Integer ear2 = Math.mod(year, 400);

    if (ear == 0 || (ear1 == 0 || ear2 == 0)) {
        return true;
    } else {
        return false;
    }
 
}
